{
  "name": "playwright-reports-local",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:dashboard": "cd dashboard && npm install && npm run build",
    "setup": "npm ci && npm run build:dashboard"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.19.0",
    "express": "^5.1.0",
    "node-cron": "^3.0.3"
  }
}
